# data_structures_final_project
Global search-and-replace modular CLI program that implements cycle detection, runtime optimization via path compression, robust input validation + error handling, and large-file processing using multiple map backends (BST, RBT, Hash).
